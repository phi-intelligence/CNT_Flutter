import 'package:flutter/material.dart';

class PrayerScreenWeb extends StatelessWidget {
  const PrayerScreenWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Prayer'),
        automaticallyImplyLeading: false,
      ),
      body: const Center(
        child: Text('Prayer Screen - Coming Soon'),
      ),
    );
  }
}

